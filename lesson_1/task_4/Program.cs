int a = 44,b = 5,c = 78;
if ( a > b)
{
    int max = a;
    Console.WriteLine("Максимальное число:" + max);
} else if(b > c)
{
    int max = b;
    Console.WriteLine("Максимальное число:" + max);
} else
{
    int max = c;
    Console.WriteLine("Максимальное число:" + max);
}